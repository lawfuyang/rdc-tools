{
  "schemaVersion": 1,
  "note": "The corpus the regression net is run over (REFERENCE §4.17). Two things are deliberately not in it. The captures themselves: hundreds of megabytes each, one command away, and not this repository's to keep. And the paths they live at: a capture is named here by a generic key (`desktop-1`, `desktop-2`, `mobile-1` -- platform, then size ascending) and identified by its SHA-256, while `captures.local.json`, which is not in git, says where this machine's copies are, and every transcript records the key rather than the path. What is checked in is what must hold about them: the transcripts under goldens/<key>/ and the labels in goldens/<key>.expect.json. `python src\\py\\rdc_analysis.py goldens --check` compares what is on this machine and reports the rest as not compared; `--write` refreshes the transcripts, and the diff of that is the point.",
  "driver": {
    "what": "the driver's own text, pinned per capture: one `batch` session per capture over that capture's `driverCommands`, its output in goldens/<key>/driver.txt. The command line in each golden names the batch file it ran, and the driver's own `#=== <line>` markers say what that file held, so the batch file itself lives under build/ and is not committed",
    "invocation": "`argv` is how the driver is invoked, and it defaults to bin/replay_dump.exe relative to the root -- the built driver. A corpus of one's own may set it to another invocation (a second build, a script), which is also how the harness itself is tested with no GPU",
    "precondition": "a built driver and a GPU. Where either is missing -- or the capture was recorded by a newer RenderDoc than the installed engine, which the driver refuses -- the driver half is reported as *not compared* rather than as a failure, so `goldens --check` still answers on a machine that cannot replay",
    "why": [
      "the driver's text was the last half compared by hand (rebuild, run a batch file, diff it in the terminal). What is pinned is what the engine answered, command by command, with the exit code and any finding on stderr",
      "one field is normalised: the header's `renderdoc` line names the *installed* engine rather than the capture's, and a golden that pinned it would fail on a machine with another RenderDoc while every answer matched. That number is judged by the version guard instead",
      "the driver's stderr reaches the golden as findings only: its per-step log lines carry seconds, the working directory and a timestamped log file name, which are this machine's run rather than the answer"
    ]
  },
  "pair": {
    "a": "mobile-1",
    "b": "desktop-1",
    "what": "the mobile-vs-desktop pair this project was started for: the same scene captured on a phone and on the desktop, which is what replaydiff and passdiff were built to compare",
    "precondition": "both bundles dumped with `--with-images` (the check runs replaydiff --with-images, so a bundle written without images fails the image counts -- which is a true statement about that bundle rather than a bug in the check)",
    "expect": {
      "same": 10,
      "removed": 2,
      "added": 37,
      "structureChanges": 10,
      "stateChanges": 10,
      "constantsChanged": 55,
      "shadersDifferent": 16,
      "imagesCompared": 2,
      "imagesNotCompared": 0
    },
    "why": [
      "10 passes align by marker path or by the innermost name, and 16 shaders differ by hash: the two frames share the mobile base pass and the sky-atmosphere LUT chain, and nothing else (the desktop frame's ray-tracing, HZB, SSR, TAA and eye-adaptation passes exist on one side only).",
      "55 constant blocks have a member whose value moved, which is the number to watch: it moves when the alignment moves, so a change in the alignment rule shows up here rather than only in a per-pass row.",
      "2 image pairs are compared and neither is left to identity: the SceneColorRendering readback both bundles hold (49.077% of its pixels differ), and one pair that is identical byte for byte."
    ]
  },
  "captures": [
    {
      "name": "desktop-1",
      "bytes": 63975987,
      "sha256": "6874db3c6981a6aac94ac07ae10432fe03ea7630a82f0b2bbb8ca5a78649b2ff",
      "api": "D3D12",
      "what": "an Unreal Engine desktop frame (RenderDoc 1.46, 2,122 chunks, 52 draw/dispatch calls, 131 markers): the desktop half of the pair, and the capture the report's pinned numbers come from (1186 events / 47 passes / 493 resources / 63 findings from 20 detectors, AGENTS.md)",
      "known": [
        "its markers balance, every draw and dispatch sits inside one, and no call is zero-work: the three file-side detectors fire nowhere in this frame",
        "the report over its bundle finds 63 things; one of them has had its cause followed to the frame and written down here, which is what turns that one from an observation into a verdict -- the other 62 stay unproven (REFERENCE §4.17)",
        {
          "detector": "dead-allocation",
          "what": "created and never used by any call",
          "evidence": "Nanite.VisibleClustersSWHW",
          "cause": "allocated for a path this frame does not run: the engine's own usage list for it is empty (usageCount 0, no first or last event), and the stream names the id only in its creation, its SetName and the capture's InitialContents section -- no call binds, copies or clears it. Two independent paths agree that nothing in the frame touches it, so the 48 MB it holds is allocated and unused",
          "verdict": "confirmed"
        },
        "1186 ids carry bound state, which is every id up to the frame's last event (1305) -- not a claim about how many are draws"
      ],
      "driverCommands": [
        ["info"],
        ["draws", "12"],
        ["textures"],
        ["debug"]
      ],
      "bundle": "build/goldens/desktop-1",
      "commands": [
        ["summary"],
        ["markers"],
        ["draws", "25"],
        ["resources", "25"],
        ["descriptors", "15"],
        ["rootsig", "8"],
        ["deps", "12"],
        ["memory", "8"],
        ["verify"],
        ["dxbc"],
        ["passdiff", "<capture>"]
      ]
    },
    {
      "name": "mobile-1",
      "bytes": 35865513,
      "sha256": "3e536c02e0c94d4ad1f9be83eb3414ce3af56cadbe577c54dbf6981fdd907ff0",
      "api": "D3D12",
      "what": "an Unreal Engine mobile (Android) frame: the smallest capture here (35.9 MB, 723 chunks, 24 draw/dispatch calls, 57 markers), which is why it is the one to try a new command on first, and the mobile half of the pair",
      "known": [
        "its markers balance, every draw and dispatch sits inside one, and no call is zero-work",
        "its marker tree nests six deep and its pass names are the mobile renderer's own (MobileSceneRender, MobileBasePass, the sky-atmosphere LUT chain)",
        "of its bundle's 35 findings the 14 unbound root parameters have their cause followed and written down here, which turns those from observations into verdicts; the other 21 stay unproven (REFERENCE §4.17)",
        {
          "detector": "unbound-root-parameter",
          "what": "the shader reads a constant block at",
          "evidence": "",
          "cause": "the reflection and the state's root parameters come from different namespaces at these events. This frame records graphics and compute on one command list (24563) and the state document reports a single `rootSignature`; at the events the finding names that is the compute signature (res2240), whose parameters are unset until the compute list sets them (chunks 261..268, before its first dispatch at 270) and which the engine reports set from its first compute event (119). So a cs shader's block is unset only in a state between dispatches; a ps or vs shader's block is declared by the *graphics* signature instead (res2286..res2293, root CBVs at b0..b5 s0) and set with SetGraphicsRootConstantBufferView before each draw (rp4..rp13; e.g. chunks 443..451 then the draw at 452). No call runs with any of these blocks unbound",
          "verdict": "not a defect"
        }
      ],
      "driverCommands": [
        ["info"],
        ["draws", "12"],
        ["textures"],
        ["debug"]
      ],
      "bundle": "build/goldens/mobile-1",
      "commands": [
        ["summary"],
        ["markers"],
        ["draws", "25"],
        ["resources", "25"],
        ["descriptors", "15"],
        ["rootsig", "8"],
        ["deps", "12"],
        ["memory", "8"],
        ["verify"],
        ["dxbc"],
        ["passdiff", "<capture>"]
      ]
    },
    {
      "name": "desktop-2",
      "bytes": 630680997,
      "sha256": "7ad0509534ead94ef33feb481837aac8ad79a0d41a1f846ae641c47403f378bb",
      "api": "D3D12",
      "what": "a D3D12 desktop frame from a renderer that is not Unreal: the largest capture here (601 MB container, 1.47 GB stream, 29,212 chunks, 50 draw/dispatch calls) and the one that exercises the newer D3D12 API variants (CreateCommittedResource3, List_Barrier, CreateAS)",
      "known": [
        "**nothing of what this frame says about itself is committed.** A transcript is the capture's own words -- its marker names, its resource names, its pass names -- and this capture's are not this repository's to publish. So it has no transcript, no label, no document, no bundle and no driver text here, and its `commands` and `driverCommands` lists are empty by design rather than by omission. What is kept is numbers and API facts, which describe the frame without quoting it",
        "the corpus still holds it by identity: its size and SHA-256 are checked whenever the file is on this machine, so a re-capture under this key is caught even though nothing it prints is compared",
        "the eid numbering does not agree with the file's chunk numbering here (REFERENCE §9): the engine's first id with pipeline state is 842 while the structured file's first draw is at chunk 316",
        "its markers balance and nest up to 37 deep, with repeated names on the way down -- which is why passdiff's rows are as long on this capture as they are. The depth is a number; the names stay out",
        "no bundle has been dumped for it, so its report-side counts are unlabelled rather than zero. A local dump would let the report be read, and its documents would stay out of the repository for the reason above"
      ],
      "commands": [],
      "driverCommands": []
    }
  ]
}
